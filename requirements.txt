# Direct dependencies of the MCAP face-redaction pipeline.
# torch/torchvision must come from the CUDA index; see setup_venv.sh.
torch
torchvision
mcap
mcap-protobuf-support
opencv-python>=4.10
numpy>=2.0
